<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Distance Calculator</title>
  <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>

  <h1>Distance of (x,y,z) from (0,0,0)</h1>
  <p>Write the code for this app which calculates<br>
     <code>d = Math.sqrt(x*x + y*y + z*z)</code></p>

  <form method="POST">
    <input type="number" step="any" name="x" placeholder="x"
           value="<%= x !== null && x !== undefined ? x : '' %>" required>
    <input type="number" step="any" name="y" placeholder="y"
           value="<%= y !== null && y !== undefined ? y : '' %>" required>
    <input type="number" step="any" name="z" placeholder="z"
           value="<%= z !== null && z !== undefined ? z : '' %>" required>
    <button type="submit">CALCULATE&nbsp;DISTANCE</button>
  </form>

  <% if (distance) { %>
    <div class="result">
      distance to (<%= x %>,<%= y %>,<%= z %>) is d = <%= distance %>
    </div>
  <% } %>

  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>

</body>
</html>
